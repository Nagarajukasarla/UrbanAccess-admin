export type LiteUser = {
    id: number;
    email: string;
    name: string;
    image?: string;
};

export type User = {
    id: number;
    email: string;
    mobile: string;
    name: string;
    image?: string;
};